# Web Context

This package renders the invoice review workflow.

- Call the API through `src/api.ts`; do not import or recreate engine logic.
- Keep the green total dollar-sign color in `--total-symbol-color`.
- Preserve upload, edit, add, remove, calculate, and visible total behavior.
- Use compact, work-focused UI patterns for accounting review tasks.
